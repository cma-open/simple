"""Common code used within the system."""

# Note - reminder (keep) no need to setup logging here
# Common code is always called from code where the system log will already have
# been instantiated. Do not call setup_system_log()
